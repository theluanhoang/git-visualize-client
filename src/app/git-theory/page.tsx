'use client';

import React from 'react';
import LessionPage from './[slug]/page'
export default function GitTheoryPage() {
  return (
   <LessionPage params={{
      slug: 'git-intro'
    }} />
  );
}